
import TakeAttendancePage from "@/app/dashboard/attendance/take/page";

export default function TeacherTakeAttendancePage() {
    // Reusing the same component for now. This can be customized later.
    return <TakeAttendancePage />;
}
